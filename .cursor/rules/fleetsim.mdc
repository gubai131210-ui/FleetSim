---
description: FleetSim 多 AGV 仿真项目开发规则
globs: **/*
alwaysApply: true
---

# FleetSim 开发规则

## 每次会话必读
- `AGENTS.md`
- `SESSION_LOG.md` 最新条目
- `docs/AGENT_SESSION_TEMPLATE.md`
- UI 相关时读 `docs/UI_GUIDELINES.md`

## 代码硬规则
1. Domain 层（`src/domain/`）禁止 `#include <Qt*>`
2. 一文件一职责，单文件 ≤300 行（硬上限 500）
3. Core/Domain 新类必须附带 Google Test
4. 架构变更需新增 `docs/decisions/` ADR
5. 不在命令行代用户编译；告知用户在 Qt Creator 测试

## UI 硬规则（禁止偷懒）
1. **MainWindow 禁止业务按钮** — 0 个 QPushButton/QToolBar 业务控件
2. **新 UI 功能 = 新 Panel 文件** — `src/ui/panels/XxxPanel.h/.cpp`
3. **centralWidget 只能是 MapView** — 禁止塞表单/曲线/表格
4. **QToolBar 禁止承载业务** — 用 QDockWidget
5. 更新 `src/ui/CMakeLists.txt` 同步新面板

## 会话结束必做
1. 按 `docs/AGENT_SESSION_TEMPLATE.md` 更新 `SESSION_LOG.md`
2. 回复用户必须含：**做了什么 / 没做什么 / 为什么 / 本地验证**
3. 跑 Reviewer 子 agent，对照偷懒自检清单

## 其他
- 车辆外观：SVG（`assets/vehicles/`）+ `VehicleGraphicsItem`
- 面板路线图见 `src/ui/panels/README.md`
